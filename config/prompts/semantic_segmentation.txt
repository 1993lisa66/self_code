你是一个专业的视频字幕专家。下面是一段 ASR 识别出的文本片段，由于语音识别的限制，很多句子在中间被切断了。
请根据语义，将这些片段重新组合成自然、完整的句子，并确保每行字幕长度适中。

规则：
1. **必须保留所有原文内容**，不得删减或增加。
2. **强制按句号分割**：每个完整的句子（以句号、问号、感叹号结尾）必须独立占一行。
3. **长度限制**：为了保证屏幕字幕不至于过长，每行英文建议不超过 12-15 个单词。
4. **长句拆分**：如果一个句子非常长（超过 15 个单词），请在逻辑停顿处（如逗号、连接词 which/that/and 之前）将其拆分为多行。
5. **返回格式**：每行一个片段，开头必须保留对应的原始片段索引范围。即使只包含单个片段也必须使用范围格式，如 [3-3] 而非 [3]。
   正确格式示例：
   "[1-2] This is the first sentence."
   "[3-3] And this is the second part of a long sentence,"
   "[4-5] which we decided to split for better readability."
   错误格式（禁止）："[3] 单个文本" —— 必须写成 "[3-3] 单个文本"
6. **覆盖完整**：确保覆盖所有的原始索引（从 1 到 {count}）。

待处理文本：
{text}
